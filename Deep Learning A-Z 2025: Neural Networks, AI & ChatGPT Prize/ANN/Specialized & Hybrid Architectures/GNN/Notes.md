CNN: designed for the data that has grid like structure ex. image 

RNN: tailored to sequence structure where each element can be connectted to two other elements most, like string of words in text 

GNN: set of objects or connections between them can be expressed as graph
	graph-based learning
	grid-based learning 
	group-based learning
	mesh-based learning	

	undirected : ex. rosa is sibling of juan like wise juan is sibling of rosa (bidirectional)
	directed : ex. rosa is older sibling of juan (one direction)

	types of graph analysis 
		graph 
		node 
		edge 

	graph convolutional network (GCN)
		convolutional layer
		non-linear activation layer: activation function RELU
		output liner layer


Applications 
	in NLP document classification
	in CV: image segmentation
	bioinformatics:genomic sequence analysis  
